---
all:
  children:
    automationcontroller:
      hosts:
        student1.sep1.demoredhat.com:

    automationhub:
      hosts:
        hub-student1.sep1.demoredhat.com:

    builder:
      hosts:
        student1.sep1.demoredhat.com:
...